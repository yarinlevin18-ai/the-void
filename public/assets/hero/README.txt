Frame 2 (HERO) glass UI assets — drop your 3 transparent PNGs here:

  dashboard.png      mini SaaS dashboard glass card
  landing-card.png   landing-page hero glass card
  component.png      UI component cluster glass card

They auto-replace the procedural placeholder cards in heroCluster
(src/main.js) the moment they exist — just hard-refresh.
Transparent background (alpha) recommended.
